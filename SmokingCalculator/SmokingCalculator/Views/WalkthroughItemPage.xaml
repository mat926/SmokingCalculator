<?xml version="1.0" encoding="UTF-8"?>
<ContentView 
             x:Class="SmokingCalculator.Views.WalkthroughItemPage"
              xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             mc:Ignorable="d">
    <ContentView.Content>
      <StackLayout>
          <!--<Label FontSize="30"
                 Text="{Binding Header}" 
                 TextColor="White"/>-->
            <Label 
                FontSize="20"
                Text="{Binding Content}" 
                TextColor="White"/>
            <Image
                Source="{Binding ImagePath}" VerticalOptions="CenterAndExpand"
                />
        </StackLayout>
  </ContentView.Content>
</ContentView>